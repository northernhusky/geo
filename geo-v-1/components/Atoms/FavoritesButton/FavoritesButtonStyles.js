import { StyleSheet } from 'react-native';

const createStyles = StyleSheet.create({
    favoritesButton: {
      fontSize: 43,
      color: '#cf1934',
      textAlign: 'center',
      marginVertical: 10,
    },
});

export default createStyles;